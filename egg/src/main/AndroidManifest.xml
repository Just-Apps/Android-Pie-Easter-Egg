<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.github.justapps.egg.pie">

    <application
            android:supportsRtl="true"
            android:theme="@style/PieEggTheme">

        <activity android:name=".PlatLogoActivity"
                  android:theme="@style/PieEggPlatLogoTheme"
                  android:configChanges="orientation|keyboardHidden"
                  android:process=":ui">
        </activity>

        <activity
                android:name=".paint.PaintActivity"
                android:configChanges="orientation|keyboardHidden|screenSize|uiMode"
                android:label="@string/app_name"
                android:theme="@style/PieEggTheme">
        </activity>

    </application>

</manifest>